// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/debian
{
  "name": "INF2003 Database Systems",
  // "image": "ghcr.io/commit2main/universal:latest",
  "dockerComposeFile": "compose.yaml",
  "service": "devcontainer",
  "features": {
    "ghcr.io/devcontainers-extra/features/apt-get-packages:1": {
      "clean_ppas": true,
      "preserve_apt_list": true,
      "packages": "mariadb-client, libmariadb-dev"
    }
  },
  "postCreateCommand": "pre-commit install && uv sync && pnpm install",
  "customizations": {
    "vscode": {
      "extensions": [
        "mtxr.sqltools",
        "mtxr.sqltools-driver-mysql",
        "ms-azuretools.vscode-documentdb"
      ]
    }
  },
  "remoteUser": "root",
  "workspaceFolder": "/workspaces/INF2003"
}
